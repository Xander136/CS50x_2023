CREATE TABLE student (
    id INTEGER,
    student_name TEXT,
    PRIMARY KEY(id)
);
CREATE TABLE houses (
    id INTEGER,
    house TEXT,
    PRIMARY KEY(id)
);
CREATE TABLE heads (
    id INTEGER,
    head TEXT,
    PRIMARY KEY(id)
);